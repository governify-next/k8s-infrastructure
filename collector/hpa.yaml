apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: governify-collector
  namespace: governify-next
  labels:
    app: governify-collector
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: governify-collector
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75