apiVersion: v1
kind: Service
metadata:
  name: governify-grafana
  namespace: governify-next
spec:
  selector:
    app: governify-grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
