kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: governify-mongo
  namespace: governify-next
  labels:
    app: governify-mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi