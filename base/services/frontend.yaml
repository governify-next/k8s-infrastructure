apiVersion: apps/v1
kind: Deployment
metadata:
  name: governify-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: governify-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: governify-frontend
    spec:
      containers:
        - name: frontend
          image: governifynext/frontend:develop
          imagePullPolicy: Always
          env:
            - {name: NODE_ENV, value: production}
            - {name: PORT, value: "3000"}
            - {name: GOV_SERVICE_NAME, value: frontend}
            - {name: GOV_LOG_LEVEL, value: INFO}
            - {name: AUTHENTICATOR_SERVICE_URL, value: "http://governify-authenticator:5800"}
            - {name: SCOPE_SERVICE_URL, value: "http://governify-scope-manager:5801"}
            - {name: REGISTRY_SERVICE_URL, value: "http://governify-registry:5802"}
            - {name: AUTH_ACCESS_COOKIE_NAME, value: governify_next_access_token}
            - {name: AUTH_REFRESH_COOKIE_NAME, value: governify_next_refresh_token}
            - {name: ACCESS_TOKEN_MAX_AGE, value: "900"}
            - {name: REFRESH_TOKEN_MAX_AGE, value: "604800"}
          ports:
            - {name: http, containerPort: 3000}
          startupProbe:
            httpGet: {path: /, port: http}
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: {path: /, port: http}
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /, port: http}
            periodSeconds: 20
          resources:
            requests: {cpu: 50m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: [ALL]}
---
apiVersion: v1
kind: Service
metadata:
  name: governify-frontend
spec:
  selector:
    app.kubernetes.io/name: governify-frontend
  ports:
    - {name: http, port: 3000, targetPort: http}
